# Java — Complete Interview Question Bank

> Section-wise, exhaustive question bank for Java interviews — from fresher/junior to senior/architect level. Covers Core Java, OOP, JVM internals, Collections, Concurrency, Java 8–21 features, Spring Boot, Hibernate/JPA, Microservices, Design Patterns, Testing, and System Design.

---

## Table of Contents

1. [Core Java Fundamentals](#1-core-java-fundamentals)
2. [Object-Oriented Programming (OOP)](#2-object-oriented-programming-oop)
3. [Java Memory Model & JVM Internals](#3-java-memory-model--jvm-internals)
4. [Garbage Collection](#4-garbage-collection)
5. [Collections Framework](#5-collections-framework)
6. [Strings in Java](#6-strings-in-java)
7. [Exception Handling](#7-exception-handling)
8. [Java 8 Features — Lambdas, Streams, Functional Programming](#8-java-8-features--lambdas-streams-functional-programming)
9. [Java 9–21 Modern Features](#9-java-921-modern-features)
10. [Multithreading & Concurrency](#10-multithreading--concurrency)
11. [Java Concurrency Utilities (java.util.concurrent)](#11-java-concurrency-utilities-javautilconcurrent)
12. [Generics & Type System](#12-generics--type-system)
13. [I/O, NIO & Serialization](#13-io-nio--serialization)
14. [Design Patterns in Java](#14-design-patterns-in-java)
15. [SOLID Principles & Clean Code](#15-solid-principles--clean-code)
16. [Spring Framework Core](#16-spring-framework-core)
17. [Spring Boot](#17-spring-boot)
18. [Spring Security](#18-spring-security)
19. [Spring Data JPA & Hibernate](#19-spring-data-jpa--hibernate)
20. [REST API Design with Spring](#20-rest-api-design-with-spring)
21. [Microservices with Spring Cloud](#21-microservices-with-spring-cloud)
22. [Database — SQL, Transactions & Connection Pooling](#22-database--sql-transactions--connection-pooling)
23. [Testing — JUnit, Mockito & Integration Testing](#23-testing--junit-mockito--integration-testing)
24. [Build Tools — Maven & Gradle](#24-build-tools--maven--gradle)
25. [Performance, Profiling & Tuning](#25-performance-profiling--tuning)
26. [Advanced & Senior-Level Questions](#26-advanced--senior-level-questions)
27. [Scenario-Based & System Design Questions](#27-scenario-based--system-design-questions)
28. [High-Frequency Quick Reference](#28-high-frequency-quick-reference)

---

## 1. Core Java Fundamentals

### Language Basics

- What are the 8 primitive data types in Java? What are their sizes?
- What is the difference between `int` and `Integer`?
- What is autoboxing and unboxing? What are its performance implications?
- What is the difference between `==` and `.equals()` in Java?
- What is the contract between `equals()` and `hashCode()`? Why must they be overridden together?
- What is the difference between `final`, `finally`, and `finalize()`?
- What does the `static` keyword mean? What can be declared static?
- What is a static initializer block? When does it run?
- What is an instance initializer block? When does it run vs a constructor?
- What is the difference between a `static` method and an instance method?
- What is `this` keyword in Java? What is `super` keyword?
- What is the difference between `pass by value` and `pass by reference`? Is Java pass-by-value?
- What is the difference between `break`, `continue`, and `return`?
- What is a labeled break/continue? Give a use case.
- What is a varargs method (`...`)? What are its rules?
- What is method overloading? What are the rules?
- What is method overriding? What are the rules?
- What is covariant return type?
- What is the difference between pre-increment (`++i`) and post-increment (`i++`)?
- What is ternary operator? What are its limitations?
- What is the `instanceof` operator? What is the pattern matching `instanceof` (Java 16+)?
- What is the difference between `>>`, `>>>`, and `<<` operators?
- What are wrapper classes? What is the Integer cache range? (-128 to 127)
- What is `NaN` in Java? How do you check for it?
- What is IEEE 754 floating-point representation? Why is `0.1 + 0.2 != 0.3`?
- What is `BigDecimal`? When should you use it over `double`?
- What is `BigInteger`?
- What is `Math.round()` vs `Math.ceil()` vs `Math.floor()`?
- What is the difference between `System.exit(0)` and `System.exit(1)`?
- What is the `assert` keyword? How do you enable assertions?

### Access Modifiers

- What are the 4 access modifiers in Java? Explain their scope.
  - `public`, `protected`, `default` (package-private), `private`
- What is the difference between `protected` and default (package-private)?
- Can a top-level class be `private`? Can it be `protected`?
- What is a package-private class? When is it useful?

### Type System

- What is the difference between a class and an interface?
- What is an abstract class? When would you use it over an interface?
- What is the difference between an abstract class and an interface after Java 8?
- What are default methods in interfaces? What problem do they solve?
- What are static methods in interfaces?
- What are private methods in interfaces (Java 9)?
- What is a marker interface? Give examples. (`Serializable`, `Cloneable`)
- What is a functional interface? What is `@FunctionalInterface`?
- What is a sealed class (Java 17)?
- What is a record (Java 16)? What does it generate?
- What is an enum in Java? Can an enum have methods and constructors?
- What is the difference between `enum` and a class with constants?
- What is the `ordinal()` and `name()` method on enums?
- What is `EnumSet` and `EnumMap`?
- What are nested classes? What are the 4 types?
  - Static nested class, inner class, local class, anonymous class

---

## 2. Object-Oriented Programming (OOP)

### Four Pillars

- What are the four pillars of OOP? Define each.
  - Encapsulation, Inheritance, Polymorphism, Abstraction
- What is encapsulation? How is it achieved in Java?
- What is data hiding? How is it different from encapsulation?
- What is inheritance? What is the `extends` keyword?
- What is the diamond problem in Java? How does Java handle it with interfaces?
- What is multiple inheritance? Does Java support it? In what form?
- What is polymorphism? What are the two types?
  - Compile-time (method overloading) and runtime (method overriding)
- What is dynamic dispatch / runtime polymorphism? How does it work?
- What is abstraction? How is it achieved in Java?

### Object Class

- What methods does every Java class inherit from `Object`?
  - `equals()`, `hashCode()`, `toString()`, `clone()`, `finalize()`, `getClass()`, `wait()`, `notify()`, `notifyAll()`
- What is `toString()`? Why should you override it?
- What is `clone()`? What is shallow clone vs deep clone?
- What is the `Cloneable` interface? What happens if you call `clone()` without implementing it?
- What is `Comparable` vs `Comparator`? When do you use each?
- What is the natural ordering of objects?

### Interfaces & Abstract Classes

- When would you choose an abstract class over an interface?
- Can an abstract class have a constructor? Can it be instantiated?
- Can an interface extend another interface?
- Can a class implement multiple interfaces?
- What is an anonymous class? When would you use one?
- What is a lambda expression compared to an anonymous class?

### Composition vs Inheritance

- What is "favor composition over inheritance"? Why?
- What is the Liskov Substitution Principle?
- What is fragile base class problem?
- What is method hiding vs method overriding?

---

## 3. Java Memory Model & JVM Internals

### JVM Architecture

- What is the JVM? What is the JRE? What is the JDK?
- What are the main components of the JVM architecture?
  - Class Loader, Runtime Data Areas, Execution Engine, Native Interface
- What are the class loaders in Java? (Bootstrap, Extension/Platform, Application)
- What is the delegation model (parent-first) in class loading?
- What is the difference between class loading, linking, and initialization?
- What is a ClassLoader? How do you implement a custom ClassLoader?
- What is `ClassNotFoundException` vs `NoClassDefFoundError`?

### JVM Memory Areas

- What are the runtime data areas in the JVM?
  - Heap, Stack, Method Area (Metaspace), PC Register, Native Method Stack
- What is the Java Heap? What are its regions?
  - Young Generation (Eden, Survivor S0/S1), Old Generation (Tenured)
- What is the Young Generation vs Old Generation?
- What is the Eden space? What are Survivor spaces?
- What is the Method Area / Metaspace? What moved from PermGen to Metaspace in Java 8?
- What is PermGen? Why was it replaced by Metaspace?
- What is the difference between Stack and Heap memory?
- What is a stack frame? What does it contain?
- What is a `StackOverflowError`? What causes it?
- What is an `OutOfMemoryError`? What are the different types?
  - Java heap space, Metaspace, GC overhead limit exceeded, Direct buffer memory
- What is off-heap memory? What is `ByteBuffer.allocateDirect()`?

### Java Memory Model (JMM)

- What is the Java Memory Model (JMM)?
- What is `volatile` keyword? What does it guarantee? What does it NOT guarantee?
- What is a happens-before relationship in the JMM?
- What is visibility problem in multithreading? How does `volatile` fix it?
- What is atomicity vs visibility vs ordering in the JMM?
- What is the difference between `volatile` and `synchronized`?
- What is memory reordering? How does the JMM prevent it?

### JIT Compilation

- What is JIT (Just-In-Time) compilation?
- What is the difference between interpreted mode and compiled mode in the JVM?
- What is C1 (client) compiler vs C2 (server) compiler? What is tiered compilation?
- What is code deoptimization? What triggers it?
- What is inlining in JIT? Why is it important?
- What is the `-server` flag? What is `-client`?
- What are JVM flags? Give 5 commonly used JVM flags.

---

## 4. Garbage Collection

### GC Fundamentals

- What is garbage collection in Java? How does the GC know what to collect?
- What is the reachability algorithm (mark-and-sweep)?
- What are GC roots? List them.
- What are strong, soft, weak, and phantom references?
- What is the difference between `WeakReference`, `SoftReference`, and `PhantomReference`?
- What is `ReferenceQueue`?
- What is a memory leak in Java? Give 4 common causes.
  - Static references, unclosed streams, ThreadLocal leaks, listener not deregistered

### GC Algorithms & Collectors

- What is the generational garbage collection hypothesis?
- What is a Minor GC vs Major GC vs Full GC?
- What is Stop-the-World (STW) pause? Why is it harmful?
- What are the GC collectors available in Java?
  - Serial GC, Parallel GC, CMS (deprecated), G1GC, ZGC, Shenandoah
- What is the Serial Garbage Collector? When is it used?
- What is the Parallel GC? What is its goal?
- What is CMS (Concurrent Mark Sweep)? Why was it deprecated?
- What is G1GC (Garbage First)? How does it work?
- What is ZGC? What is its design goal? (sub-millisecond pauses)
- What is Shenandoah GC?
- What is Epsilon GC? When would you use it?
- How do you choose a GC for your application?
- What is GC tuning? What JVM flags are used? (`-Xmx`, `-Xms`, `-Xmn`, `-XX:+UseG1GC`)
- What is `-Xmx` vs `-Xms`? Why is it recommended to set them equal in production?
- What is `-XX:+PrintGCDetails`? What is `-verbose:gc`?
- What is GC log analysis? What tools do you use?
- What is `jstat`? How do you use it to monitor GC?

---

## 5. Collections Framework

### Core Hierarchy

- What is the Java Collections Framework?
- What is the difference between `Collection` and `Collections`?
- What is the hierarchy of the Collections interface?
  - `Collection` → `List`, `Set`, `Queue`, `Deque`; `Map` is separate
- What is the difference between `List`, `Set`, and `Map`?
- What is `Iterable`? What is `Iterator`? What is `ListIterator`?
- What is fail-fast vs fail-safe iterators?
- What is `ConcurrentModificationException`? When does it occur?

### List

- What is `ArrayList`? What is its internal data structure?
- What is the initial capacity of `ArrayList`? How does it grow? (50% more)
- What is `LinkedList`? When would you use it over `ArrayList`?
- What is the time complexity of `get()`, `add()`, `remove()` for ArrayList vs LinkedList?
- What is `Vector`? How is it different from `ArrayList`?
- What is `CopyOnWriteArrayList`? When is it used?
- What is `Arrays.asList()`? What are its limitations?
- What is `List.of()`? How is it different from `Arrays.asList()`?

### Set

- What is `HashSet`? How is it implemented internally? (backed by HashMap)
- What is `LinkedHashSet`? What order does it maintain?
- What is `TreeSet`? What ordering does it use? What is the time complexity?
- What is `EnumSet`? Why is it extremely efficient?
- What is `CopyOnWriteArraySet`?
- Can `HashSet` contain `null`? Can `TreeSet` contain `null`?

### Map

- What is `HashMap`? How does it work internally?
- How does `HashMap` handle collisions? (chaining with linked list / red-black tree)
- What is the load factor in `HashMap`? What is the default? (0.75)
- What is the initial capacity of `HashMap`? (16)
- When does `HashMap` resize? What is the treeification threshold? (8)
- What is `HashMap` behavior in Java 8 vs Java 7 (treeification of buckets)?
- What happens if two keys have the same `hashCode()`?
- What is `LinkedHashMap`? What iteration order does it maintain?
- What is `TreeMap`? What is its data structure? (Red-Black Tree)
- What is `WeakHashMap`? When is it useful?
- What is `IdentityHashMap`?
- What is `ConcurrentHashMap`? How is it thread-safe without locking the entire map?
- What was the segmentation approach in ConcurrentHashMap before Java 8? What changed in Java 8?
- What is `Hashtable`? Why is it considered obsolete?
- What is `EnumMap`?
- What is `Map.of()` and `Map.copyOf()`?
- What is `computeIfAbsent()`, `computeIfPresent()`, `merge()`, `getOrDefault()`?

### Queue & Deque

- What is `Queue`? What is `Deque`?
- What is `PriorityQueue`? How does it order elements?
- What is `ArrayDeque`? When is it better than `Stack`?
- What is `BlockingQueue`? What are its implementations?
  - `LinkedBlockingQueue`, `ArrayBlockingQueue`, `PriorityBlockingQueue`, `DelayQueue`, `SynchronousQueue`
- What is `SynchronousQueue`?
- What is `DelayQueue`? When would you use it?

### Utility & Algorithms

- What is `Collections.sort()` vs `Arrays.sort()`? What algorithm does each use?
- What is `Collections.synchronizedList()`? Is it truly thread-safe?
- What is `Collections.unmodifiableList()`?
- What is `Collections.emptyList()`, `singletonList()`?
- What is the difference between `Comparable` and `Comparator` in sorting?
- What is `Comparator.comparing()` and method chaining with `.thenComparing()`?

---

## 6. Strings in Java

- What is the `String` class? Is it mutable or immutable?
- Why is `String` immutable in Java? What are the benefits?
- What is the String pool (String intern pool)? How does it work?
- What is `String.intern()`? When would you use it?
- What is the difference between `==` and `.equals()` for Strings?
- What is `StringBuilder` vs `StringBuffer`? When do you use each?
- What is the time complexity of String concatenation with `+` in a loop? How do you fix it?
- What does the compiler do with String concatenation using `+`? (uses StringBuilder)
- What is `String.format()` vs `String.valueOf()` vs `toString()`?
- What are the common String methods? (`charAt`, `substring`, `indexOf`, `split`, `replace`, `trim`, `toCharArray`, `toLowerCase`, `toUpperCase`)
- What is the difference between `substring()` before and after Java 7u6 (memory leak fix)?
- What is `String.join()` and `StringJoiner`?
- What is a `CharSequence`? What implements it?
- What is `String.chars()` in Java 8?
- What are text blocks (Java 15)?
- What is `String.isBlank()` vs `String.isEmpty()` (Java 11)?
- What are `String.strip()`, `stripLeading()`, `stripTrailing()` (Java 11)?
- What is `String.repeat()` (Java 11)?
- What is `String.lines()` (Java 11)?

---

## 7. Exception Handling

- What is the exception hierarchy in Java?
  - `Throwable` → `Error` / `Exception` → `RuntimeException`
- What is the difference between `Error` and `Exception`?
- What is the difference between checked and unchecked exceptions?
- When should you use checked vs unchecked exceptions?
- What is `try-with-resources`? What interface must a resource implement? (`AutoCloseable`)
- What is `finally` block? Is it always executed?
- What is multi-catch syntax (`catch (ExA | ExB e)`)?
- What is the difference between `throw` and `throws`?
- What is exception chaining? What is `initCause()` and cause constructor?
- What is a custom exception? How do you create one?
- What is `NullPointerException`? How do you avoid it?
- What is `StackOverflowError`? What causes it?
- What is `ClassCastException`? When does it occur?
- What is `ArrayIndexOutOfBoundsException` vs `IndexOutOfBoundsException`?
- What is `IllegalArgumentException` vs `IllegalStateException`?
- What is `ConcurrentModificationException`?
- What is `UnsupportedOperationException`?
- What is exception swallowing? Why is it dangerous?
- What is the `try-finally` pattern for resource cleanup?
- What is the difference between `Exception` in `catch` block and the re-thrown exception?
- What happens if `finally` block throws an exception?
- Should you catch `Throwable`? Should you catch `Error`?

---

## 8. Java 8 Features — Lambdas, Streams, Functional Programming

### Lambda Expressions

- What is a lambda expression? What is its syntax?
- What is a functional interface? Give 5 examples from `java.util.function`.
  - `Predicate<T>`, `Function<T,R>`, `Supplier<T>`, `Consumer<T>`, `BiFunction<T,U,R>`
- What is `@FunctionalInterface`? What happens if you add two abstract methods?
- What is a method reference? What are the 4 types?
  - Static method ref, instance method ref (bound), instance method ref (unbound), constructor ref
- What is the difference between a lambda and an anonymous class?
- Can a lambda capture local variables? What constraints apply? (effectively final)
- What is closure in Java lambda context?
- What is the difference between `Predicate.and()`, `Predicate.or()`, `Predicate.negate()`?
- What is `Function.andThen()` vs `Function.compose()`?

### Stream API

- What is the Stream API? How is it different from `Collection`?
- What are intermediate operations vs terminal operations in streams?
- What are lazy evaluation and short-circuit operations in streams?
- What is the difference between `map()` and `flatMap()`?
- What is `filter()`? What is `distinct()`? What is `sorted()`?
- What is `peek()`? When would you use it?
- What is `limit()` and `skip()`?
- What is `reduce()`? Give an example.
- What is `collect()`? What is a `Collector`?
- What are `Collectors.toList()`, `toSet()`, `toMap()`, `groupingBy()`, `partitioningBy()`, `joining()`, `counting()`, `summarizingInt()`?
- What is `Stream.of()` vs `Arrays.stream()` vs `Collection.stream()`?
- What is `IntStream`, `LongStream`, `DoubleStream`? When do you use them?
- What is `Stream.generate()` vs `Stream.iterate()`?
- What is `anyMatch()`, `allMatch()`, `noneMatch()`?
- What is `findFirst()` vs `findAny()`? When does `findAny()` differ?
- What is `min()`, `max()`, `count()`?
- What is `toArray()` on a stream?
- What is `mapToInt()`, `mapToLong()`, `mapToDouble()`? What is `average()`?
- What is a parallel stream? How does it work internally? What are its risks?
- When should you use a parallel stream? When should you NOT?
- What is `Spliterator`?
- What is `Stream.takeWhile()` and `Stream.dropWhile()` (Java 9)?
- Can you reuse a stream? What happens if you try?
- What is the difference between `stream()` and `parallelStream()`?

### Optional

- What is `Optional<T>`? Why was it introduced?
- What is `Optional.of()` vs `Optional.ofNullable()` vs `Optional.empty()`?
- What is `isPresent()` vs `ifPresent()`?
- What is `orElse()` vs `orElseGet()` vs `orElseThrow()`?
- What is `map()` and `flatMap()` on `Optional`?
- What is `filter()` on `Optional`?
- What is `Optional.or()` (Java 9)?
- What is `ifPresentOrElse()` (Java 9)?
- What is `Optional.stream()` (Java 9)?
- When should you NOT use `Optional`? (as method parameters, as fields, for primitives)

### Date & Time API (java.time)

- What is the `java.time` package? Why was it introduced? (`java.util.Date` problems)
- What is `LocalDate`, `LocalTime`, `LocalDateTime`?
- What is `ZonedDateTime`? What is `OffsetDateTime`?
- What is `Instant`? What is it used for?
- What is `Duration` vs `Period`?
- What is `DateTimeFormatter`?
- What is `ZoneId`?
- How do you convert between `java.util.Date` and `java.time.LocalDateTime`?
- What is `TemporalAdjuster`?
- What is `Clock`? How is it useful in testing?

---

## 9. Java 9–21 Modern Features

### Java 9

- What are modules in Java 9 (JPMS — Java Platform Module System)?
- What is `module-info.java`?
- What is `requires`, `exports`, `opens`, `uses`, `provides` in a module descriptor?
- What is the difference between named module and unnamed module?
- What are `private` interface methods (Java 9)?
- What is `Stream.takeWhile()`, `dropWhile()`, `ofNullable()` (Java 9)?
- What are immutable collection factory methods? (`List.of()`, `Set.of()`, `Map.of()`)
- What is the `Optional` `ifPresentOrElse()`, `or()`, `stream()` (Java 9)?
- What is `CompletableFuture` improvements in Java 9?

### Java 10–13

- What is local variable type inference (`var`) in Java 10? What are its limitations?
- Can `var` be used as a method return type? As a field?
- What is `var` in lambda parameters (Java 11)?
- What are the new `String` methods in Java 11? (`isBlank`, `strip`, `lines`, `repeat`)
- What is `HttpClient` in Java 11?
- What is `Files.readString()` and `Files.writeString()` (Java 11)?
- What is Switch Expressions (preview Java 12, standard Java 14)?

### Java 14–17

- What are Records (Java 16)? What do they auto-generate?
- What are the limitations of Records?
- What is Pattern Matching for `instanceof` (Java 16)?
- What are Sealed Classes (Java 17)? What is `permits`?
- What is `sealed`, `non-sealed`, `final` in class hierarchy?
- What are Text Blocks (Java 15)?
- What are helpful `NullPointerException` messages (Java 14)?

### Java 18–21

- What are Virtual Threads (Project Loom, Java 21)? What problem do they solve?
- How are virtual threads different from platform threads?
- What is the thread-per-request model vs reactive model? How do virtual threads change the equation?
- What is `Thread.ofVirtual()`?
- What are Structured Concurrency (Java 21 preview)? What is `StructuredTaskScope`?
- What are Sequenced Collections (Java 21)?
- What is Pattern Matching for `switch` (Java 21)?
- What are Record Patterns (Java 21)?
- What is Scoped Values (Java 21 preview)?
- What is `String Templates` (preview Java 21)?
- What is Foreign Function & Memory API (Java 21)?

---

## 10. Multithreading & Concurrency

### Thread Fundamentals

- What is a thread? How does it differ from a process?
- What are the 6 thread states in Java? (`NEW`, `RUNNABLE`, `BLOCKED`, `WAITING`, `TIMED_WAITING`, `TERMINATED`)
- What is `Thread.start()` vs `Thread.run()`?
- What are two ways to create a thread in Java? (extend `Thread`, implement `Runnable`)
- What is `Callable` vs `Runnable`? What is the difference?
- What is a daemon thread? How do you make a thread a daemon?
- What is `Thread.sleep()` vs `Object.wait()`? What is the difference?
- What is `Thread.join()`? What does it do?
- What is `Thread.interrupt()`? What is `isInterrupted()` vs `interrupted()`?
- What is `InterruptedException`? Should you swallow it?
- What is `Thread.yield()`? What does it do?
- What is thread priority? Can you rely on it?
- What is a race condition? Give an example.
- What is a critical section?

### Synchronization

- What is the `synchronized` keyword?
- What is an intrinsic lock / monitor lock?
- What is the difference between a synchronized method and a synchronized block?
- What is the difference between object-level lock and class-level lock?
- What is `wait()`, `notify()`, `notifyAll()`? Why must they be called in a synchronized block?
- What is `notifyAll()` vs `notify()`? When would spurious wakeups occur?
- What is a deadlock? How does it occur? Give an example.
- What are the four conditions for a deadlock? (Coffman conditions)
- How do you prevent/avoid a deadlock?
- What is a livelock? How is it different from a deadlock?
- What is thread starvation?
- What is `volatile`? What does it guarantee? What doesn't it guarantee?
- What is the difference between `volatile` and `synchronized`?
- What is `double-checked locking`? Is it safe? (need `volatile` in Java 5+)
- What is the `happens-before` relationship? Give 3 examples.

### Thread Safety

- What does it mean for a class to be thread-safe?
- What are immutable objects? Why are they inherently thread-safe?
- What is the read-modify-write problem?
- What is a check-then-act race condition?
- What is the producer-consumer problem? How do you implement it in Java?
- What is the readers-writers problem?
- What is thread confinement?
- What is `ThreadLocal`? What is its use case? What is its memory leak risk?
- What is `InheritableThreadLocal`?

---

## 11. Java Concurrency Utilities (java.util.concurrent)

### Locks

- What is `ReentrantLock`? How is it different from `synchronized`?
- What is `tryLock()`? What is its use case?
- What is `lockInterruptibly()`?
- What is `ReentrantReadWriteLock`? When do you use it?
- What is `StampedLock` (Java 8)? What modes does it support?
- What is `ReadWriteLock` vs `ReentrantLock`?
- What is `Condition`? How is it used with `ReentrantLock`?

### Atomic Classes

- What is the `java.util.concurrent.atomic` package?
- What is `AtomicInteger`? What is `compareAndSet()`?
- What is CAS (Compare-And-Swap)? How does it work at hardware level?
- What is `AtomicReference`?
- What is `AtomicLong`, `AtomicBoolean`?
- What is `LongAdder`? When is it better than `AtomicLong`?
- What is `LongAccumulator`?

### Executor Framework

- What is the Executor framework? Why is it better than creating raw threads?
- What is `ExecutorService`? How do you shut it down gracefully?
- What is the difference between `shutdown()` and `shutdownNow()`?
- What is `ThreadPoolExecutor`? What are its core parameters?
  - corePoolSize, maximumPoolSize, keepAliveTime, workQueue, threadFactory, handler
- What are the thread pool types created by `Executors`?
  - `newFixedThreadPool`, `newCachedThreadPool`, `newSingleThreadExecutor`, `newScheduledThreadPool`
- What is the problem with `Executors.newCachedThreadPool()` under heavy load?
- What are the `RejectedExecutionHandler` policies?
  - `AbortPolicy`, `CallerRunsPolicy`, `DiscardPolicy`, `DiscardOldestPolicy`
- What is `Future<T>`? What is `get()`, `cancel()`, `isDone()`, `isCancelled()`?
- What is `CompletableFuture`? What problem does it solve over `Future`?
- What is `CompletableFuture.supplyAsync()`, `thenApply()`, `thenAccept()`, `thenRun()`?
- What is `thenCompose()` vs `thenCombine()`?
- What is `allOf()` vs `anyOf()` in `CompletableFuture`?
- What is `exceptionally()`, `handle()`, `whenComplete()` for error handling?
- What is `ForkJoinPool`? What is work-stealing?
- What is a `RecursiveTask` vs `RecursiveAction`?
- What is the common ForkJoinPool (used by parallel streams)?

### Concurrent Collections

- What is `ConcurrentHashMap`? How does it differ from `HashMap` and `Hashtable`?
- What is `CopyOnWriteArrayList`? When would you use it?
- What is `BlockingQueue`? What are the key implementations?
- What is `LinkedBlockingQueue` vs `ArrayBlockingQueue`?
- What is `DelayQueue`?
- What is `ConcurrentLinkedQueue`?
- What is `ConcurrentSkipListMap`?

### Synchronization Barriers

- What is `CountDownLatch`? Give a use case.
- What is `CyclicBarrier`? How is it different from `CountDownLatch`?
- What is `Semaphore`? Give a use case. (connection pool, rate limiter)
- What is `Exchanger`?
- What is `Phaser` (Java 7)?

---

## 12. Generics & Type System

- What are generics in Java? What problem do they solve?
- What is type erasure? What does the compiler do with generics at runtime?
- What is a generic class vs a generic method?
- What is a bounded type parameter? (`<T extends Comparable<T>>`)
- What is a wildcard? What is `?`?
- What is upper bounded wildcard (`? extends T`) vs lower bounded wildcard (`? super T`)?
- What is the PECS rule? (Producer Extends, Consumer Super)
- Can you create an array of a generic type? Why not?
- What is `@SuppressWarnings("unchecked")`?
- What is a raw type? Why is it dangerous?
- What is `List<Object>` vs `List<?>`? Why can't you assign `List<String>` to `List<Object>`?
- What is covariance and contravariance in Java generics?
- What is reification? What is a reifiable type?
- What is a generic singleton factory pattern?
- What is type inference in Java 7 diamond operator (`<>`) and Java 8 method type inference?

---

## 13. I/O, NIO & Serialization

### Classic I/O

- What is the difference between `InputStream`/`OutputStream` (byte stream) and `Reader`/`Writer` (char stream)?
- What is `FileInputStream` vs `BufferedInputStream`? Why use buffering?
- What is `FileReader` vs `FileWriter`? When do you use them?
- What is `ObjectInputStream` / `ObjectOutputStream`?
- What is `PrintWriter` vs `PrintStream`?

### NIO & NIO.2

- What is Java NIO? How is it different from classic I/O?
- What is a `Channel` in NIO?
- What is a `Buffer` in NIO? What is `ByteBuffer`?
- What is `flip()`, `rewind()`, `clear()`, `compact()` on a Buffer?
- What is `Selector` in NIO? What is non-blocking I/O?
- What is the `Path` API (Java 7)? What is `Files` utility?
- What is `Files.walk()`, `Files.list()`?
- What is `WatchService`?
- What is `AsynchronousFileChannel`?

### Serialization

- What is serialization in Java? What is `Serializable`?
- What is `serialVersionUID`? What happens if it's missing or mismatched?
- What is `transient` keyword? What fields should be marked transient?
- What is `Externalizable`? How is it different from `Serializable`?
- What is the `readObject()` / `writeObject()` method?
- What are the security risks of Java serialization?
- What are alternatives to Java serialization? (JSON, Protobuf, Avro, Kryo)

---

## 14. Design Patterns in Java

### Creational Patterns

- What is the Singleton pattern? How many ways can you implement it in Java?
  - Eager initialization, Lazy initialization, Synchronized method, Double-checked locking, Bill Pugh (static holder), Enum singleton
- What is the best way to implement a Singleton in Java? Why Enum is preferred?
- What is the Factory Method pattern? Give a Java example.
- What is the Abstract Factory pattern? How is it different from Factory Method?
- What is the Builder pattern? When do you use it? (Lombok `@Builder`)
- What is the Prototype pattern?

### Structural Patterns

- What is the Adapter pattern? Give a Java example.
- What is the Decorator pattern? Give a Java example. (Java I/O uses it)
- What is the Facade pattern? Give a Java example.
- What is the Proxy pattern? What are the types?
  - Static proxy, JDK dynamic proxy, CGLIB proxy (used by Spring AOP)
- What is the Composite pattern?
- What is the Flyweight pattern? Where is it used in Java? (`Integer.valueOf()`, `String.intern()`)
- What is the Bridge pattern?

### Behavioral Patterns

- What is the Strategy pattern? Give a Java example.
- What is the Observer pattern? How does it relate to Java's event model?
- What is the Template Method pattern?
- What is the Command pattern?
- What is the Chain of Responsibility pattern?
- What is the Iterator pattern? How does Java's `Iterator` interface implement it?
- What is the State pattern?
- What is the Mediator pattern?
- What is the Visitor pattern?
- What is the Memento pattern?
- What is the Interpreter pattern?

### Java-Specific Patterns

- What is the Null Object pattern?
- What is the Dependency Injection pattern?
- What is the Service Locator pattern? Why is it considered an anti-pattern?
- What is the DAO (Data Access Object) pattern?
- What is the Repository pattern? How is it different from DAO?
- What is the DTO (Data Transfer Object) pattern?
- What is the MVC (Model-View-Controller) pattern?

---

## 15. SOLID Principles & Clean Code

- What are the SOLID principles? Define each.
  - S: Single Responsibility Principle
  - O: Open/Closed Principle
  - L: Liskov Substitution Principle
  - I: Interface Segregation Principle
  - D: Dependency Inversion Principle
- What is the Single Responsibility Principle? Give a Java example of violation and fix.
- What is the Open/Closed Principle? How do you implement it using interfaces/abstraction?
- What is the Liskov Substitution Principle? What is the classic `Rectangle/Square` violation?
- What is the Interface Segregation Principle? Give an example of a fat interface.
- What is the Dependency Inversion Principle? How does Spring DI implement it?
- What is DRY (Don't Repeat Yourself)?
- What is YAGNI (You Ain't Gonna Need It)?
- What is KISS (Keep It Simple, Stupid)?
- What is cohesion? What is coupling? What is high cohesion / low coupling?
- What is code smell? Give 5 examples.
- What is refactoring?
- What is technical debt?

---

## 16. Spring Framework Core

### IoC & Dependency Injection

- What is the Spring Framework? What problem does it solve?
- What is IoC (Inversion of Control)? What is the IoC container?
- What is Dependency Injection? What are the 3 types of DI in Spring?
  - Constructor injection, Setter injection, Field injection
- Which DI type is recommended and why? (Constructor injection)
- What is the `ApplicationContext`? How is it different from `BeanFactory`?
- What is a Spring Bean? What are its lifecycle phases?
- What is `@Component`, `@Service`, `@Repository`, `@Controller`? How are they related?
- What is `@Autowired`? How does Spring resolve ambiguity?
- What is `@Qualifier`? When do you use it?
- What is `@Primary`?
- What is `@Bean`? How is it different from `@Component`?
- What is `@Configuration`? What is a Java-based configuration class?
- What is `@Import`?

### Bean Scopes & Lifecycle

- What are the Spring bean scopes? (`singleton`, `prototype`, `request`, `session`, `application`, `websocket`)
- What is the difference between `singleton` scope and the Singleton design pattern?
- What is `prototype` scope? What is its lifecycle gotcha?
- What is `@Scope("prototype")`?
- What is `@Lazy`?
- What are Spring bean lifecycle callbacks?
  - `@PostConstruct`, `@PreDestroy`, `InitializingBean`, `DisposableBean`, `init-method`, `destroy-method`
- What is `BeanFactoryPostProcessor` vs `BeanPostProcessor`?
- What is `ApplicationContextAware`?
- What is `@DependsOn`?

### Spring AOP

- What is AOP (Aspect-Oriented Programming)?
- What is a cross-cutting concern? Give examples. (logging, security, transaction, caching)
- What is an Aspect, Advice, Pointcut, JoinPoint, and Weaving?
- What are the types of advice in Spring AOP?
  - `@Before`, `@After`, `@AfterReturning`, `@AfterThrowing`, `@Around`
- What is `@Aspect`?
- What is the difference between Spring AOP and AspectJ?
- How does Spring AOP work? (JDK dynamic proxy vs CGLIB)
- When is JDK proxy used vs CGLIB? (interface vs class)
- What is `@EnableAspectJAutoProxy`?
- What is `ProceedingJoinPoint`?

---

## 17. Spring Boot

### Core Concepts

- What is Spring Boot? How does it differ from Spring Framework?
- What is auto-configuration? How does it work?
- What is `@SpringBootApplication`? What 3 annotations does it combine?
  - `@Configuration`, `@EnableAutoConfiguration`, `@ComponentScan`
- What is `spring.factories` / `AutoConfiguration.imports` (Spring Boot 3)?
- What is `@ConditionalOnClass`, `@ConditionalOnMissingBean`, `@ConditionalOnProperty`?
- What is `@EnableAutoConfiguration`? Can you disable specific auto-configurations?
- What are Spring Boot starters? How do they simplify dependency management?
- What is `application.properties` vs `application.yml`?
- What is `@ConfigurationProperties`? How is it better than `@Value`?
- What is `@Value`? What is Spring Expression Language (SpEL)?
- What are Spring Profiles? What is `@Profile`?
- What is `spring.profiles.active`?
- What are property sources? What is the property override order?
- What is an embedded server? Which ones does Spring Boot support? (Tomcat, Jetty, Undertow)
- What is `CommandLineRunner` vs `ApplicationRunner`?
- What is `@EventListener`? What is `ApplicationEvent`?
- What is Spring Boot DevTools?

### Actuator & Monitoring

- What is Spring Boot Actuator?
- What are the common Actuator endpoints? (`/health`, `/info`, `/metrics`, `/env`, `/beans`, `/mappings`, `/loggers`, `/threaddump`, `/heapdump`)
- How do you expose/secure Actuator endpoints?
- What is `management.endpoints.web.exposure.include`?
- What is a custom Actuator endpoint?
- What is Micrometer? How does it integrate with Spring Boot Actuator?
- How do you expose metrics to Prometheus using Spring Boot?

### Spring Boot Testing

- What is `@SpringBootTest`? What does it do?
- What is `@WebMvcTest`? What does it test in isolation?
- What is `@DataJpaTest`?
- What is `@MockBean` vs `@Mock`?
- What is `MockMvc`? How do you use it to test a REST controller?
- What is `TestRestTemplate`?
- What is `WebTestClient`?
- What is `@TestConfiguration`?
- What is `@ActiveProfiles` in tests?
- What is `@DirtiesContext`?

---

## 18. Spring Security

- What is Spring Security? What are its core features?
- What is the Spring Security filter chain?
- What is `SecurityFilterChain`?
- What is `UserDetailsService`? What is `UserDetails`?
- What is `PasswordEncoder`? Why should you use BCrypt?
- What is `BCryptPasswordEncoder`?
- What is authentication vs authorization in Spring Security?
- What is `@PreAuthorize`, `@PostAuthorize`, `@Secured`?
- What is `@EnableMethodSecurity`?
- What is `@EnableWebSecurity`?
- What is CSRF protection in Spring Security? When should you disable it?
- What is CORS in Spring Security? How do you configure it?
- How do you implement JWT authentication in Spring Boot?
- What is `OncePerRequestFilter`? How do you use it for JWT validation?
- What is `AuthenticationProvider`?
- What is `SecurityContext` and `SecurityContextHolder`?
- What is OAuth2 in Spring Security?
- What is `@AuthenticationPrincipal`?
- What is role-based access control in Spring Security?
- What is the difference between `hasRole()` and `hasAuthority()`?
- What is `permitAll()` vs `authenticated()` vs `denyAll()`?
- What is session management in Spring Security? What is `SessionCreationPolicy.STATELESS`?

---

## 19. Spring Data JPA & Hibernate

### JPA / Hibernate Fundamentals

- What is JPA? What is Hibernate? How are they related?
- What is an `EntityManager`? What is the `PersistenceContext`?
- What are the 4 JPA entity states? (`Transient`, `Managed`, `Detached`, `Removed`)
- What is the difference between `persist()`, `merge()`, `remove()`, `detach()`, `refresh()`?
- What is a `@Entity`? What is `@Table`?
- What is `@Id`? What is `@GeneratedValue`? What are the 4 generation strategies?
  - `AUTO`, `IDENTITY`, `SEQUENCE`, `TABLE`
- What is `@Column`? What are its attributes?
- What is `@Transient`?
- What is `@Embeddable` and `@Embedded`?
- What is `@MappedSuperclass`?
- What is inheritance mapping in JPA? What are the strategies?
  - `SINGLE_TABLE`, `TABLE_PER_CLASS`, `JOINED`

### Relationships

- What is `@OneToOne`, `@OneToMany`, `@ManyToOne`, `@ManyToMany`?
- What is `mappedBy`? What is the owning side of a relationship?
- What is `cascade`? What are the cascade types?
  - `ALL`, `PERSIST`, `MERGE`, `REMOVE`, `REFRESH`, `DETACH`
- What is `orphanRemoval`?
- What is `fetch`? What is `FetchType.LAZY` vs `FetchType.EAGER`?
- What is the N+1 query problem? How do you fix it? (`JOIN FETCH`, `@BatchSize`, `@EntityGraph`)
- What is `@EntityGraph`?
- What is `@NamedEntityGraph`?

### Performance & Caching

- What is the first-level cache (session cache) in Hibernate?
- What is the second-level cache in Hibernate? How do you enable it? (EhCache, Caffeine)
- What is the query cache in Hibernate?
- What is `@Cache` and `@Cacheable` in Hibernate?
- What is lazy loading? What is `LazyInitializationException`? How do you fix it?
- What is the Open Session in View anti-pattern?
- What is dirty checking in Hibernate? How does it work?
- What is `@DynamicUpdate`? When is it useful?
- What is optimistic locking in JPA? What is `@Version`?
- What is pessimistic locking? What is `LockModeType`?
- What is `@Transactional`? How does Spring handle it?
- What is the difference between `REQUIRED`, `REQUIRES_NEW`, `NESTED`, `SUPPORTS`, `MANDATORY`, `NOT_SUPPORTED`, `NEVER` propagation?
- What are transaction isolation levels?
  - `READ_UNCOMMITTED`, `READ_COMMITTED`, `REPEATABLE_READ`, `SERIALIZABLE`
- What is a dirty read, phantom read, non-repeatable read?
- What is `ddl-auto`? What are the values? Why should `update` never be used in production?
- What is Flyway? What is Liquibase? Why should you use them?

### Spring Data JPA

- What is Spring Data JPA? What is `JpaRepository`?
- What is the repository hierarchy? (`CrudRepository`, `PagingAndSortingRepository`, `JpaRepository`)
- What are derived query methods? Give 5 examples.
- What is `@Query`? When do you use JPQL vs native query?
- What is `@Modifying`? When is it required?
- What is `@Param`?
- What is pagination in Spring Data? What is `Pageable` and `Page<T>`?
- What is `Slice<T>` vs `Page<T>`?
- What is `@EntityGraph` in Spring Data JPA?
- What is `Specification<T>`? When do you use it?
- What is `QueryDSL`?
- What is a `Projection` in Spring Data JPA?
- What is `@Lock`?
- What is auditing in Spring Data JPA? (`@CreatedDate`, `@LastModifiedDate`, `@EnableJpaAuditing`)

---

## 20. REST API Design with Spring

- What is `@RestController` vs `@Controller`?
- What is `@RequestMapping`? What is `@GetMapping`, `@PostMapping`, etc.?
- What is `@PathVariable` vs `@RequestParam` vs `@RequestBody`?
- What is `@RequestHeader`?
- What is `ResponseEntity<T>`? When do you use it?
- What is `@ResponseStatus`?
- What is `@ExceptionHandler`? What is `@ControllerAdvice` / `@RestControllerAdvice`?
- How do you implement global exception handling in Spring Boot?
- What is `ProblemDetail` (RFC 7807, Spring 6)?
- What is `@Valid` and `@Validated`? How do you validate request bodies?
- What annotations does Bean Validation provide? (`@NotNull`, `@NotBlank`, `@Size`, `@Min`, `@Max`, `@Email`, `@Pattern`)
- What is `BindingResult`?
- What is content negotiation in Spring MVC?
- What is `@CrossOrigin`? How do you configure CORS globally?
- What is HTTP message converter? What is `MappingJackson2HttpMessageConverter`?
- What is `@JsonProperty`, `@JsonIgnore`, `@JsonInclude`, `@JsonSerialize`, `@JsonDeserialize`?
- What is Jackson ObjectMapper? How do you configure it globally?
- How do you implement pagination and sorting in a REST API with Spring?
- How do you implement HATEOAS in Spring Boot? (`spring-boot-starter-hateoas`)
- What is versioning of REST APIs? How do you implement it in Spring?
- How do you implement file upload in Spring Boot? (`MultipartFile`)
- How do you implement file download with streaming in Spring Boot?
- What is `@Async`? How do you enable async execution?
- What is `@Scheduled`? How do you enable scheduling?

---

## 21. Microservices with Spring Cloud

### Core Concepts

- What is a microservices architecture? Pros and cons vs monolith?
- What is Spring Cloud?
- What is service discovery? What is Eureka?
- What is an API Gateway? What is Spring Cloud Gateway?
- What is a circuit breaker? What is Resilience4j? What is Hystrix (deprecated)?
- What is `@CircuitBreaker`? What is fallback?
- What is bulkhead pattern?
- What is rate limiting with Resilience4j?
- What is retry with Resilience4j?
- What is `@Retry`? What is `@RateLimiter`? What is `@Bulkhead`?
- What is a load balancer? What is Spring Cloud LoadBalancer?
- What is `RestTemplate` vs `WebClient`? Which is preferred in modern Spring?
- What is `OpenFeign`? What is `@FeignClient`?
- What is Spring Cloud Config Server?
- What is distributed tracing? What is Micrometer Tracing (formerly Sleuth)?
- What is Zipkin? How does it integrate with Spring Boot?
- What is Correlation ID / Trace ID? How do you propagate it?

### Messaging

- What is Spring Kafka? What is `@KafkaListener`?
- What is Spring AMQP? What is RabbitMQ integration?
- What is `@RabbitListener`?
- What is an event-driven microservice?
- What is the outbox pattern?
- What is the saga pattern in microservices?

---

## 22. Database — SQL, Transactions & Connection Pooling

- What is JDBC? What are the key JDBC objects? (`DriverManager`, `Connection`, `Statement`, `ResultSet`)
- What is `PreparedStatement`? Why is it better than `Statement`?
- What is SQL injection? How does `PreparedStatement` prevent it?
- What is a database transaction? What are ACID properties?
- What is `Connection.setAutoCommit(false)`?
- What are the transaction isolation levels in SQL?
- What is a dirty read, non-repeatable read, phantom read?
- What is connection pooling? Why is it critical in a Java application?
- What is HikariCP? Why is it the default in Spring Boot?
- What are the key HikariCP configuration parameters?
  - `maximumPoolSize`, `minimumIdle`, `connectionTimeout`, `idleTimeout`, `maxLifetime`
- What is `DataSource`? What is `JdbcTemplate`?
- What is `NamedParameterJdbcTemplate`?
- What is database migration? Why is Flyway/Liquibase better than `ddl-auto`?
- What is Flyway? How do you version SQL migration scripts?
- What is `schema.sql` vs `data.sql` in Spring Boot?

---

## 23. Testing — JUnit, Mockito & Integration Testing

### JUnit 5

- What is JUnit 5? What are the 3 modules? (`JUnit Platform`, `JUnit Jupiter`, `JUnit Vintage`)
- What is `@Test`, `@BeforeEach`, `@AfterEach`, `@BeforeAll`, `@AfterAll`?
- What is `@Disabled`?
- What is `@DisplayName`?
- What is `@ParameterizedTest`? What are the source annotations? (`@ValueSource`, `@CsvSource`, `@MethodSource`, `@EnumSource`)
- What is `@RepeatedTest`?
- What is `@Nested`? How do you use it?
- What is `@Tag`? How do you run tagged tests?
- What is `Assertions.assertAll()`?
- What is `assertThrows()`?
- What is `@TempDir`?
- What is `@ExtendWith`?
- What is `@TestMethodOrder`?

### Mockito

- What is Mockito? What is a mock?
- What is the difference between mock, stub, and spy?
- What is `@Mock`, `@InjectMocks`, `@Spy`, `@Captor`?
- What is `when(...).thenReturn(...)` vs `doReturn(...).when(...)`?
- What is `verify()`? How do you verify method calls?
- What is `ArgumentCaptor`? How do you use it?
- What is `@MockBean` in Spring Boot?
- What is `Mockito.any()`, `eq()`, `anyString()` matchers?
- What is `doThrow()`, `doNothing()`?
- What is mocking static methods in Mockito 3.4+? (`MockedStatic`)
- What is mocking constructors in Mockito 3.4+?

### Integration Testing

- What is the difference between unit tests, integration tests, and end-to-end tests?
- What is `@SpringBootTest`? What is `webEnvironment = RANDOM_PORT`?
- What is `TestContainers`? How do you use it for database integration tests?
- What is WireMock? How do you use it to mock external HTTP services?
- What is `@DataJpaTest`? What database does it use by default?
- What is an H2 in-memory database? What are its limitations for integration tests?
- What is contract testing? What is Pact?
- What is `MockMvc`? How do you test REST endpoints with it?

---

## 24. Build Tools — Maven & Gradle

### Maven

- What is Maven? What is a POM (Project Object Model)?
- What is the Maven build lifecycle? What are the phases?
  - `validate`, `compile`, `test`, `package`, `verify`, `install`, `deploy`
- What is the difference between `compile`, `test`, `provided`, `runtime`, and `import` scopes?
- What is a Maven plugin? What is a goal?
- What is `mvn clean install`? What does each phase do?
- What is a SNAPSHOT dependency? How is it different from a release?
- What is the Maven local repository vs remote repository vs central repository?
- What is a parent POM? What is the Spring Boot parent POM?
- What is a BOM (Bill of Materials)?
- What is `dependencyManagement` vs `dependencies`?
- What is Maven wrapper (`mvnw`)?

### Gradle

- What is Gradle? How is it different from Maven?
- What is `build.gradle` vs `build.gradle.kts`?
- What is `settings.gradle`?
- What is a Gradle task?
- What is the Gradle build lifecycle? (`initialization`, `configuration`, `execution`)
- What is `implementation` vs `api` vs `compileOnly` vs `runtimeOnly` in Gradle?
- What is the Gradle wrapper (`gradlew`)?
- Why is Gradle generally faster than Maven? (incremental builds, build cache, daemon)

---

## 25. Performance, Profiling & Tuning

### Profiling & Tooling

- What is `jps`? What does it show?
- What is `jstack`? How do you use it to diagnose a deadlock?
- What is `jmap`? How do you take a heap dump?
- What is `jstat`? How do you monitor GC activity?
- What is `jcmd`?
- What is VisualVM? What can you do with it?
- What is JMC (Java Mission Control)? What is JFR (Java Flight Recorder)?
- What is async-profiler?
- What is `jconsole`?

### Performance Tuning

- What are the common causes of high CPU usage in a Java app?
- What are common causes of memory issues in Java?
- How do you analyze a thread dump? What patterns indicate a deadlock?
- How do you analyze a heap dump? What tool do you use? (MAT, VisualVM)
- What is a memory leak in Java? How do you find one?
- What is GC pressure? What causes it?
- How do you tune the JVM heap for a production application?
- What is `-XX:+HeapDumpOnOutOfMemoryError`?
- What is `-XX:HeapDumpPath`?
- What is `String deduplication` in G1GC? (`-XX:+UseStringDeduplication`)
- What is the JVM warm-up period? How do you handle it in production?
- What is connection pool exhaustion? How do you diagnose it?
- How do you benchmark Java code? What is JMH (Java Microbenchmark Harness)?
- What is the JVM ergonomics feature?

---

## 26. Advanced & Senior-Level Questions

### Language Internals

- How does the JVM execute a Java program from source to machine code?
- What is bytecode? What is the `.class` file?
- What is `javap`? How do you use it to inspect bytecode?
- What is dynamic dispatch? How is it implemented in bytecode? (`invokevirtual`, `invokeinterface`, `invokespecial`, `invokestatic`, `invokedynamic`)
- What is `invokedynamic`? How does it enable lambda expressions?
- What is a hidden class (Java 15)?
- What is string concatenation in Java 9+? (uses `invokedynamic` + `StringConcatFactory`)
- What is the `VarHandle` API (Java 9)?
- What is the `MethodHandle` API?
- What are stackwalking APIs (Java 9)?

### Concurrency Deep Dives

- What is the ABA problem in CAS-based algorithms?
- What is lock-free vs wait-free algorithms?
- What is a false sharing problem in CPU cache? How do you fix it? (`@Contended`)
- What is `sun.misc.Unsafe`? What is its role?
- What is the `LockSupport` class? What is `park()` and `unpark()`?
- What is work-stealing in ForkJoinPool?
- What is structured concurrency (Java 21)?
- What is the difference between virtual threads and reactive programming (Project Reactor)?
- What are the limitations of virtual threads? (pinning to carrier thread, non-preemptable)
- What is carrier thread pinning? What causes it with virtual threads?

### JVM & GC Advanced

- What is NUMA (Non-Uniform Memory Access) awareness in the JVM?
- What is compressed OOPs? What is `-XX:+UseCompressedOops`?
- What is safepoint in the JVM? What operations require a safepoint?
- What is time-to-safepoint (TTSP)?
- What is GC Ergonomics?
- What is remembered set in G1GC?
- What is card table in GC?
- What is write barrier in GC?
- What is region-based heap layout in G1GC/ZGC?

---

## 27. Scenario-Based & System Design Questions

### Design Problems (Java-Specific)

- Design a **thread-safe LRU Cache** in Java. Use `LinkedHashMap` + `ReentrantReadWriteLock` or `ConcurrentHashMap`.
- Design a **thread pool** from scratch using `BlockingQueue` and `Thread`.
- Design a **connection pool** in Java.
- Design a **rate limiter** in Java (token bucket or sliding window).
- Design a **distributed task scheduler** using Spring Boot and Quartz/DB.
- Design an **event-driven notification system** using Spring Boot + Kafka.
- Design a **circuit breaker** from scratch in Java.
- Design a **pub/sub event bus** using `BlockingQueue` and `ExecutorService`.
- Design a **generic retry mechanism** with exponential backoff in Java.

### Debugging Scenarios

- Your Spring Boot app is throwing `LazyInitializationException` in production. What are the causes and fixes?
- Your application is experiencing a deadlock under load. How do you diagnose and fix it?
- Your service's response time degrades over time. How do you investigate? (GC pressure, memory leak, thread pool exhaustion)
- Your `@Transactional` method is not rolling back on a runtime exception. What are the common reasons?
- Your `ConcurrentHashMap` is still causing race conditions. What might be wrong?
- Your application is throwing `OutOfMemoryError: Java heap space`. Walk through your investigation.
- Your Hibernate app is running hundreds of queries per request. How do you diagnose and fix it? (N+1 problem)
- Your JWT tokens are not being validated. You get 401 on every request. Walk through the debug.
- Your `CompletableFuture` exceptions are being silently swallowed. How do you fix it?

### Architecture Questions

- How would you design a high-throughput order processing system using Java and Spring Boot?
- How do you ensure idempotency in a Java REST API?
- How do you implement distributed locking in a Java microservice?
- How do you handle database migrations across 10 microservices in a zero-downtime deployment?
- How would you implement a read-heavy API that serves millions of requests per day?

---

## 28. High-Frequency Quick Reference

Questions that appear in **nearly every Java interview**, sorted by importance:

| # | Question | Frequency |
|---|---|---|
| 1 | How does `HashMap` work internally? Collision resolution, resizing, treeification. | 🔴 Always |
| 2 | What is the difference between `==` and `.equals()`? `hashCode()` contract. | 🔴 Always |
| 3 | What is the difference between `String`, `StringBuilder`, `StringBuffer`? | 🔴 Always |
| 4 | What is a deadlock? How do you prevent it? | 🔴 Always |
| 5 | What is `synchronized` vs `volatile`? | 🔴 Always |
| 6 | What is the difference between `ArrayList` and `LinkedList`? | 🔴 Always |
| 7 | What is Java 8 Stream API? `map()` vs `flatMap()`. | 🔴 Always |
| 8 | What is a functional interface? Give examples. | 🔴 Always |
| 9 | What is `Optional`? How do you use it? | 🔴 Always |
| 10 | What is the JVM memory model? Heap vs Stack vs Metaspace. | 🔴 Always |
| 11 | What are checked vs unchecked exceptions? | 🔴 Always |
| 12 | What is `@Transactional`? How does propagation work? | 🔴 Always |
| 13 | What is the N+1 problem in Hibernate? How do you fix it? | 🔴 Always |
| 14 | What is `FetchType.LAZY` vs `EAGER`? | 🔴 Always |
| 15 | What is Spring IoC and Dependency Injection? | 🔴 Always |
| 16 | What is `@SpringBootApplication`? Auto-configuration? | 🔴 Always |
| 17 | What is `ConcurrentHashMap`? Why is `HashMap` unsafe in threads? | 🔴 Always |
| 18 | What are the SOLID principles? | 🔴 Always |
| 19 | What is thread lifecycle and thread states? | 🔴 Always |
| 20 | What is garbage collection? What is G1GC? | 🟠 Very Likely |
| 21 | What is `CompletableFuture`? How does async work in Java? | 🟠 Very Likely |
| 22 | What is the difference between `interface` and `abstract class` (post-Java 8)? | 🟠 Very Likely |
| 23 | What are virtual threads? When do you use them (Java 21)? | 🟠 Very Likely |
| 24 | What is `@Query` in Spring Data JPA? JPQL vs native? | 🟠 Very Likely |
| 25 | What is `ExecutorService`? What are thread pool types? | 🟠 Very Likely |
| 26 | What is immutability? Why is it important for concurrency? | 🟠 Very Likely |
| 27 | What is JPA entity lifecycle? Transient, Managed, Detached, Removed. | 🟠 Very Likely |
| 28 | What are design patterns? Explain Singleton, Factory, Builder, Strategy. | 🟠 Very Likely |
| 29 | What is Spring Security filter chain? How do you implement JWT? | 🟠 Very Likely |
| 30 | What are Records, Sealed Classes, Pattern Matching (Java 14–17)? | 🟡 Likely |
| 31 | What is `StampedLock`? When over `ReentrantReadWriteLock`? | 🟡 Senior Level |
| 32 | What is `invokedynamic`? How do lambdas compile? | 🟡 Senior Level |
| 33 | What is safepoint in JVM? What is TTSP? | 🟡 Senior Level |
| 34 | What is `LongAdder` vs `AtomicLong` under contention? | 🟡 Senior Level |
| 35 | What is structured concurrency (Java 21)? | 🟡 Senior Level |
